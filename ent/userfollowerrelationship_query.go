// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/0xfzz/tuwitt/ent/predicate"
	"github.com/0xfzz/tuwitt/ent/useraccount"
	"github.com/0xfzz/tuwitt/ent/userfollowerrelationship"
)

// UserFollowerRelationshipQuery is the builder for querying UserFollowerRelationship entities.
type UserFollowerRelationshipQuery struct {
	config
	ctx           *QueryContext
	order         []userfollowerrelationship.OrderOption
	inters        []Interceptor
	predicates    []predicate.UserFollowerRelationship
	withFollower  *UserAccountQuery
	withFollowing *UserAccountQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserFollowerRelationshipQuery builder.
func (ufrq *UserFollowerRelationshipQuery) Where(ps ...predicate.UserFollowerRelationship) *UserFollowerRelationshipQuery {
	ufrq.predicates = append(ufrq.predicates, ps...)
	return ufrq
}

// Limit the number of records to be returned by this query.
func (ufrq *UserFollowerRelationshipQuery) Limit(limit int) *UserFollowerRelationshipQuery {
	ufrq.ctx.Limit = &limit
	return ufrq
}

// Offset to start from.
func (ufrq *UserFollowerRelationshipQuery) Offset(offset int) *UserFollowerRelationshipQuery {
	ufrq.ctx.Offset = &offset
	return ufrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ufrq *UserFollowerRelationshipQuery) Unique(unique bool) *UserFollowerRelationshipQuery {
	ufrq.ctx.Unique = &unique
	return ufrq
}

// Order specifies how the records should be ordered.
func (ufrq *UserFollowerRelationshipQuery) Order(o ...userfollowerrelationship.OrderOption) *UserFollowerRelationshipQuery {
	ufrq.order = append(ufrq.order, o...)
	return ufrq
}

// QueryFollower chains the current query on the "follower" edge.
func (ufrq *UserFollowerRelationshipQuery) QueryFollower() *UserAccountQuery {
	query := (&UserAccountClient{config: ufrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ufrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ufrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userfollowerrelationship.Table, userfollowerrelationship.FieldID, selector),
			sqlgraph.To(useraccount.Table, useraccount.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userfollowerrelationship.FollowerTable, userfollowerrelationship.FollowerColumn),
		)
		fromU = sqlgraph.SetNeighbors(ufrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFollowing chains the current query on the "following" edge.
func (ufrq *UserFollowerRelationshipQuery) QueryFollowing() *UserAccountQuery {
	query := (&UserAccountClient{config: ufrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ufrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ufrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(userfollowerrelationship.Table, userfollowerrelationship.FieldID, selector),
			sqlgraph.To(useraccount.Table, useraccount.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, userfollowerrelationship.FollowingTable, userfollowerrelationship.FollowingColumn),
		)
		fromU = sqlgraph.SetNeighbors(ufrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first UserFollowerRelationship entity from the query.
// Returns a *NotFoundError when no UserFollowerRelationship was found.
func (ufrq *UserFollowerRelationshipQuery) First(ctx context.Context) (*UserFollowerRelationship, error) {
	nodes, err := ufrq.Limit(1).All(setContextOp(ctx, ufrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userfollowerrelationship.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ufrq *UserFollowerRelationshipQuery) FirstX(ctx context.Context) *UserFollowerRelationship {
	node, err := ufrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserFollowerRelationship ID from the query.
// Returns a *NotFoundError when no UserFollowerRelationship ID was found.
func (ufrq *UserFollowerRelationshipQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ufrq.Limit(1).IDs(setContextOp(ctx, ufrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userfollowerrelationship.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ufrq *UserFollowerRelationshipQuery) FirstIDX(ctx context.Context) int {
	id, err := ufrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserFollowerRelationship entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserFollowerRelationship entity is found.
// Returns a *NotFoundError when no UserFollowerRelationship entities are found.
func (ufrq *UserFollowerRelationshipQuery) Only(ctx context.Context) (*UserFollowerRelationship, error) {
	nodes, err := ufrq.Limit(2).All(setContextOp(ctx, ufrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userfollowerrelationship.Label}
	default:
		return nil, &NotSingularError{userfollowerrelationship.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ufrq *UserFollowerRelationshipQuery) OnlyX(ctx context.Context) *UserFollowerRelationship {
	node, err := ufrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserFollowerRelationship ID in the query.
// Returns a *NotSingularError when more than one UserFollowerRelationship ID is found.
// Returns a *NotFoundError when no entities are found.
func (ufrq *UserFollowerRelationshipQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ufrq.Limit(2).IDs(setContextOp(ctx, ufrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userfollowerrelationship.Label}
	default:
		err = &NotSingularError{userfollowerrelationship.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ufrq *UserFollowerRelationshipQuery) OnlyIDX(ctx context.Context) int {
	id, err := ufrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserFollowerRelationships.
func (ufrq *UserFollowerRelationshipQuery) All(ctx context.Context) ([]*UserFollowerRelationship, error) {
	ctx = setContextOp(ctx, ufrq.ctx, "All")
	if err := ufrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserFollowerRelationship, *UserFollowerRelationshipQuery]()
	return withInterceptors[[]*UserFollowerRelationship](ctx, ufrq, qr, ufrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ufrq *UserFollowerRelationshipQuery) AllX(ctx context.Context) []*UserFollowerRelationship {
	nodes, err := ufrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserFollowerRelationship IDs.
func (ufrq *UserFollowerRelationshipQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ufrq.ctx.Unique == nil && ufrq.path != nil {
		ufrq.Unique(true)
	}
	ctx = setContextOp(ctx, ufrq.ctx, "IDs")
	if err = ufrq.Select(userfollowerrelationship.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ufrq *UserFollowerRelationshipQuery) IDsX(ctx context.Context) []int {
	ids, err := ufrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ufrq *UserFollowerRelationshipQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ufrq.ctx, "Count")
	if err := ufrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ufrq, querierCount[*UserFollowerRelationshipQuery](), ufrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ufrq *UserFollowerRelationshipQuery) CountX(ctx context.Context) int {
	count, err := ufrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ufrq *UserFollowerRelationshipQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ufrq.ctx, "Exist")
	switch _, err := ufrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ufrq *UserFollowerRelationshipQuery) ExistX(ctx context.Context) bool {
	exist, err := ufrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserFollowerRelationshipQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ufrq *UserFollowerRelationshipQuery) Clone() *UserFollowerRelationshipQuery {
	if ufrq == nil {
		return nil
	}
	return &UserFollowerRelationshipQuery{
		config:        ufrq.config,
		ctx:           ufrq.ctx.Clone(),
		order:         append([]userfollowerrelationship.OrderOption{}, ufrq.order...),
		inters:        append([]Interceptor{}, ufrq.inters...),
		predicates:    append([]predicate.UserFollowerRelationship{}, ufrq.predicates...),
		withFollower:  ufrq.withFollower.Clone(),
		withFollowing: ufrq.withFollowing.Clone(),
		// clone intermediate query.
		sql:  ufrq.sql.Clone(),
		path: ufrq.path,
	}
}

// WithFollower tells the query-builder to eager-load the nodes that are connected to
// the "follower" edge. The optional arguments are used to configure the query builder of the edge.
func (ufrq *UserFollowerRelationshipQuery) WithFollower(opts ...func(*UserAccountQuery)) *UserFollowerRelationshipQuery {
	query := (&UserAccountClient{config: ufrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ufrq.withFollower = query
	return ufrq
}

// WithFollowing tells the query-builder to eager-load the nodes that are connected to
// the "following" edge. The optional arguments are used to configure the query builder of the edge.
func (ufrq *UserFollowerRelationshipQuery) WithFollowing(opts ...func(*UserAccountQuery)) *UserFollowerRelationshipQuery {
	query := (&UserAccountClient{config: ufrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ufrq.withFollowing = query
	return ufrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserFollowerRelationship.Query().
//		GroupBy(userfollowerrelationship.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ufrq *UserFollowerRelationshipQuery) GroupBy(field string, fields ...string) *UserFollowerRelationshipGroupBy {
	ufrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserFollowerRelationshipGroupBy{build: ufrq}
	grbuild.flds = &ufrq.ctx.Fields
	grbuild.label = userfollowerrelationship.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.UserFollowerRelationship.Query().
//		Select(userfollowerrelationship.FieldCreatedAt).
//		Scan(ctx, &v)
func (ufrq *UserFollowerRelationshipQuery) Select(fields ...string) *UserFollowerRelationshipSelect {
	ufrq.ctx.Fields = append(ufrq.ctx.Fields, fields...)
	sbuild := &UserFollowerRelationshipSelect{UserFollowerRelationshipQuery: ufrq}
	sbuild.label = userfollowerrelationship.Label
	sbuild.flds, sbuild.scan = &ufrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserFollowerRelationshipSelect configured with the given aggregations.
func (ufrq *UserFollowerRelationshipQuery) Aggregate(fns ...AggregateFunc) *UserFollowerRelationshipSelect {
	return ufrq.Select().Aggregate(fns...)
}

func (ufrq *UserFollowerRelationshipQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ufrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ufrq); err != nil {
				return err
			}
		}
	}
	for _, f := range ufrq.ctx.Fields {
		if !userfollowerrelationship.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ufrq.path != nil {
		prev, err := ufrq.path(ctx)
		if err != nil {
			return err
		}
		ufrq.sql = prev
	}
	return nil
}

func (ufrq *UserFollowerRelationshipQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserFollowerRelationship, error) {
	var (
		nodes       = []*UserFollowerRelationship{}
		_spec       = ufrq.querySpec()
		loadedTypes = [2]bool{
			ufrq.withFollower != nil,
			ufrq.withFollowing != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserFollowerRelationship).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserFollowerRelationship{config: ufrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ufrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ufrq.withFollower; query != nil {
		if err := ufrq.loadFollower(ctx, query, nodes, nil,
			func(n *UserFollowerRelationship, e *UserAccount) { n.Edges.Follower = e }); err != nil {
			return nil, err
		}
	}
	if query := ufrq.withFollowing; query != nil {
		if err := ufrq.loadFollowing(ctx, query, nodes, nil,
			func(n *UserFollowerRelationship, e *UserAccount) { n.Edges.Following = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ufrq *UserFollowerRelationshipQuery) loadFollower(ctx context.Context, query *UserAccountQuery, nodes []*UserFollowerRelationship, init func(*UserFollowerRelationship), assign func(*UserFollowerRelationship, *UserAccount)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserFollowerRelationship)
	for i := range nodes {
		fk := nodes[i].FollowerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(useraccount.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "follower_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ufrq *UserFollowerRelationshipQuery) loadFollowing(ctx context.Context, query *UserAccountQuery, nodes []*UserFollowerRelationship, init func(*UserFollowerRelationship), assign func(*UserFollowerRelationship, *UserAccount)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*UserFollowerRelationship)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(useraccount.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ufrq *UserFollowerRelationshipQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ufrq.querySpec()
	_spec.Node.Columns = ufrq.ctx.Fields
	if len(ufrq.ctx.Fields) > 0 {
		_spec.Unique = ufrq.ctx.Unique != nil && *ufrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ufrq.driver, _spec)
}

func (ufrq *UserFollowerRelationshipQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userfollowerrelationship.Table, userfollowerrelationship.Columns, sqlgraph.NewFieldSpec(userfollowerrelationship.FieldID, field.TypeInt))
	_spec.From = ufrq.sql
	if unique := ufrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ufrq.path != nil {
		_spec.Unique = true
	}
	if fields := ufrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userfollowerrelationship.FieldID)
		for i := range fields {
			if fields[i] != userfollowerrelationship.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ufrq.withFollower != nil {
			_spec.Node.AddColumnOnce(userfollowerrelationship.FieldFollowerID)
		}
		if ufrq.withFollowing != nil {
			_spec.Node.AddColumnOnce(userfollowerrelationship.FieldUserID)
		}
	}
	if ps := ufrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ufrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ufrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ufrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ufrq *UserFollowerRelationshipQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ufrq.driver.Dialect())
	t1 := builder.Table(userfollowerrelationship.Table)
	columns := ufrq.ctx.Fields
	if len(columns) == 0 {
		columns = userfollowerrelationship.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ufrq.sql != nil {
		selector = ufrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ufrq.ctx.Unique != nil && *ufrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ufrq.predicates {
		p(selector)
	}
	for _, p := range ufrq.order {
		p(selector)
	}
	if offset := ufrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ufrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserFollowerRelationshipGroupBy is the group-by builder for UserFollowerRelationship entities.
type UserFollowerRelationshipGroupBy struct {
	selector
	build *UserFollowerRelationshipQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ufrgb *UserFollowerRelationshipGroupBy) Aggregate(fns ...AggregateFunc) *UserFollowerRelationshipGroupBy {
	ufrgb.fns = append(ufrgb.fns, fns...)
	return ufrgb
}

// Scan applies the selector query and scans the result into the given value.
func (ufrgb *UserFollowerRelationshipGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ufrgb.build.ctx, "GroupBy")
	if err := ufrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserFollowerRelationshipQuery, *UserFollowerRelationshipGroupBy](ctx, ufrgb.build, ufrgb, ufrgb.build.inters, v)
}

func (ufrgb *UserFollowerRelationshipGroupBy) sqlScan(ctx context.Context, root *UserFollowerRelationshipQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ufrgb.fns))
	for _, fn := range ufrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ufrgb.flds)+len(ufrgb.fns))
		for _, f := range *ufrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ufrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ufrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserFollowerRelationshipSelect is the builder for selecting fields of UserFollowerRelationship entities.
type UserFollowerRelationshipSelect struct {
	*UserFollowerRelationshipQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ufrs *UserFollowerRelationshipSelect) Aggregate(fns ...AggregateFunc) *UserFollowerRelationshipSelect {
	ufrs.fns = append(ufrs.fns, fns...)
	return ufrs
}

// Scan applies the selector query and scans the result into the given value.
func (ufrs *UserFollowerRelationshipSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ufrs.ctx, "Select")
	if err := ufrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserFollowerRelationshipQuery, *UserFollowerRelationshipSelect](ctx, ufrs.UserFollowerRelationshipQuery, ufrs, ufrs.inters, v)
}

func (ufrs *UserFollowerRelationshipSelect) sqlScan(ctx context.Context, root *UserFollowerRelationshipQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ufrs.fns))
	for _, fn := range ufrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ufrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ufrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
